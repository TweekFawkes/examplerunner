runner:
  build:
    url: https://github.com/TweekFawkes/examplerunner.git
    directory: /001/
  
  # Assume one input that will be optionally passed to the app.py as a command line argument

  run:
    name: app.py

  # Assume stdout and stderr outputs
